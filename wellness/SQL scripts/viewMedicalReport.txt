DELIMITER $$
CREATE PROCEDURE `viewMedicalReport_procedure`(In user_Id int)
BEGIN

set @userId=(select userId from user where userId=user_Id);
select * from MedicalReport where userId=@userId;

END$$
DELIMITER ;
